services:
  - type: static_site
    name: skyblue-project
    buildCommand: echo "Static HTML — no build needed"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
