services:
  - type: static_site
    name: window-cleaner-interview
    buildCommand: echo "Static HTML — no build needed"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
